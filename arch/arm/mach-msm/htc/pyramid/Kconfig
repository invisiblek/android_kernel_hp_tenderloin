config BOARD_HEADER_FILE
	string "HTC board specific header file name"
	default ""

config MACH_PYRAMID
	depends on ARCH_MSM8X60
	select MACH_HTC
	bool "HTC Pyramid"
	help
	  Support for the HTC PYRAMID device.
